# AGENTS.md

## Project Focus

This repository is a datasource-focused TVmaze GraphQL candidate exercise. Keep candidate-facing work centered on `src/datasources/tvmaze-api.ts`.

## Engineering Rules

- Use `pnpm` for dependency installation and scripts.
- Preserve strict TypeScript settings.
- Keep the resolver and mapper as reference implementations for this exercise version.
- Keep docs candidate-facing and avoid leaking solution code into `README.md`.
- Update `CHANGELOG.md` for meaningful exercise changes.

## Verification

```bash
pnpm run typecheck
pnpm test
```
